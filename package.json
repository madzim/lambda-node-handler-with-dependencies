{
  "name": "lambda-node-handler-with-dependencies",
  "version": "1.0.0",
  "description": "POC for NodeJS AWS Lambda Handler with dependencies",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "package" : "rm package.zip; zip -r -q package.zip index.js .env node_modules/*",
    "listpkg" : "unzip -l package.zip | more"
  },
  "author": "",
  "engines": {
    "node": ">=0.10.36"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^2.0.0"
  }
}
